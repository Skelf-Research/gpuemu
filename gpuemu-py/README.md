# gpuemu-py

Python client package for `gpuemu`.

## Install

```bash
pip install -e ./gpuemu-py
```

## Includes

- daemon client transport
- validation helpers
- seeded fuzzing helpers
- framework adapters
